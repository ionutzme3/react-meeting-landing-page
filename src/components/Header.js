import React from "react";
import Hero from "./Hero";
import Logo from "./Logo"

const Header = () => {
    return (
        <header>
            <Logo />
            <Hero />
        </header>
    );
};

export default Header;